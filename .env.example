# Sweet Tooth local development
# Supabase: Saadia-Asghar's Project (tnoyspplfqalgtbskwgy, Tokyo)
# URL-encode # in password as %23 — use port 5432 (session pooler)

DATABASE_URL=postgresql://postgres.tnoyspplfqalgtbskwgy:[YOUR_PASSWORD_ENCODED]@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres

# API server
PORT=8080
PUBLIC_API_URL=http://localhost:8080

# Frontend (separate terminal: PORT=20458)
BASE_PATH=/

# WhatsApp Cloud API (optional — for live webhook)
WHATSAPP_VERIFY_TOKEN=change-me-to-a-random-string
WHATSAPP_ACCESS_TOKEN=

# OpenAI embeddings (optional — falls back to local hash embeddings)
# OPENAI_API_KEY=

# Google Cloud Vision API key (optional — falls back to mock receipt verification)
# GOOGLE_CLOUD_VISION_API_KEY=

