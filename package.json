{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.5.2",
  "scripts": {
    "preinstall": "node -e \"if (!process.env.npm_config_user_agent?.includes('pnpm') && !process.env.VERCEL) process.exit(0)\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "vercel-build": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/sweet-tooth run build",
    "baileys:bridge": "docker compose -f docker-compose.baileys.yml up --build",
    "baileys:bridge:down": "docker compose -f docker-compose.baileys.yml down",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "@esbuild/win32-x64": "^0.27.3",
    "@rollup/rollup-win32-x64-msvc": "4.62.2",
    "prettier": "^3.9.4",
    "typescript": "~5.9.3"
  }
}
